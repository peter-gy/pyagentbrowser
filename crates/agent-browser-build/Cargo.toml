[package]
name = "agent-browser-build"
version = "0.1.0"
edition = "2021"
description = "Audited source adaptation for the embedded agent-browser engine"
license = "Apache-2.0"
publish = false

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
tempfile = "3"
